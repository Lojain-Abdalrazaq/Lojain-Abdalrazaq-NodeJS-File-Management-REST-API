<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>File Manager</title>
    <!-- including fonts-->
    <link href='https://fonts.googleapis.com/css?family=Nunito Sans' rel='stylesheet'>
    <!-- including styles -->
    <link href="/index.css" rel="stylesheet" type="text/css"/>
  </head>

  <body>
    <header>
      <!-- Display the title of the application -->
      <div class="title-index-page">
        <h2>File Management REST API</h2>
      </div>
    </header>

    <main>
      
      <!-- Display the list of files -->
      <div class="list-files-all">
        <p>Available Files</p>
        <% if (files.length > 0) { %>
          <table class="file-table">
            <tbody>
              <% files.forEach(file => { %>
                <tr>
                  <td><a href="/files/<%= file %>"><%= file %></a></td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        <% } else { %>
          <p>No Files Available For Listing</p>
        <% } %>
      </div>

      <!-- Create a new file -->
      <div id="create-File-button">
        <button href="/create" class="button">Create New File</button>
      </div>

    </main>
  </body>
</html>
